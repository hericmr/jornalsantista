# Supabase Configuration
VITE_SUPABASE_URL=your_supabase_project_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key

# Login de admin é feito via Supabase Auth (Authentication > Users no
# dashboard do Supabase), com o papel controlado pela tabela `profiles`
# (coluna role = 'admin'). Não há mais credenciais aqui.